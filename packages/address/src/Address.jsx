import React from 'react';
import styled from 'styled-components';

const AddressWrapper = styled.div`
  padding: 20px;
  font-size: 2rem;
`;

export default function Address({}) {
  return <AddressWrapper>Address Widget</AddressWrapper>;
}
